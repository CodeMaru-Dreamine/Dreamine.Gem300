namespace Dreamine.Gem300;

/// <summary>
/// \if KO
/// <para>Dreamine GEM300 구현 어셈블리를 검색하거나 등록할 때 사용하는 표식입니다.</para>
/// \endif
/// \if EN
/// <para>Marks the assembly boundary used to discover or register Dreamine GEM300 implementations.</para>
/// \endif
/// </summary>
public sealed class Gem300AssemblyMarker
{
    private Gem300AssemblyMarker() { }
}
